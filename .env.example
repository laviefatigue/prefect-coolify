# Database Configuration
POSTGRES_PASSWORD=prefect_secure_password_2024

# Optional: Additional PostgreSQL settings
POSTGRES_USER=prefect
POSTGRES_DB=prefect

# Optional: Redis settings (usually defaults are fine)
REDIS_PASSWORD=

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
PREFECT_LOGGING_LEVEL=INFO

# Note: Coolify will automatically handle domain assignment
# No need to set PREFECT_API_URL manually